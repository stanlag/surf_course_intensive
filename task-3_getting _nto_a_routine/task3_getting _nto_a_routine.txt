

function подготовить_завтрак() {
    if (есть_яйца == true && есть_колбаса == true) {
        пожарить_яишницу_с_колбасой();
    } else if (есть_мюсли == true && есть_молоко == true) {
        приготовить_мюсли_с_молоком();
    } else {
        идти_в_магазин();
        купить_еду();
    }
}

function пожарить_яишницу_с_колбасой() {
    // Функция яишницы
}

function приготовить_мюсли_с_молоком() {
    // Функция готовки мюслей с молоком
}

function идти_в_магазин() {
    // Пойти в магазин для покупки продуктов
}

function купить_яишницу_с_колбасой() {
    // Функция покупки яиц и колбасы
}

function купить_мюсли_с_молоком() {
    // Функция покупки мюслей с молоком
}

function купить_еду() {
    // Определяем степень закупки
    switch (степень голода) {
        case 'не очень голодный':
            купить_мюсли_с_молоком();
            break;
        case 'очень голодный':
            купить_яишницу_с_колбасой();
            break;
        case 'Съел бы слона':
            купить_мюсли_с_молоком();
            купить_яишницу_с_колбасой();
            break;
        default:
            купить_яишницу_с_колбасой();
    }
}

void main() {
    // Главная часть программы
    while (true) {
        подготовить_завтрак(); // Действие повторяется каждый день
    }
}
